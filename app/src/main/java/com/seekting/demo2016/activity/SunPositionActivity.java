package com.seekting.demo2016.activity;

import android.os.Bundle;

public class SunPositionActivity extends BaseActivity {
    static {
        MainActivity.putTitle(SunPositionActivity.class, "太阳的位置");
        MainActivity.putDesc(SunPositionActivity.class, "");
    }

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);


    }

}
